/**
 * Temporary feature toggles — flip and rebuild to re-enable.
 */

// Gates: auto-flagging a submission as 'flagged' at submit time
// (survey-form.ts), and the manual 🚩 flag button on My Submissions
// (submissions-list.ts/html). Disabled per request — set true to restore.
export const FLAGGING_ENABLED = false;
